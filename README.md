# Temp Mailer Bot

MVP принимает входящие письма через SMTP и показывает их в Telegram‑боте. У каждого пользователя свой временный адрес: бот выводит главное меню с адресом, количеством писем и датой создания ящика, инлайн‑кнопки позволяют обновить список, сменить адрес или открыть конкретное письмо.

## Стек
- SMTP: `aiosmtpd`
- Telegram: `python-telegram-bot`
- Хранение: SQLite (`tempmail.db`)

## Подготовка
```powershell
python -m venv .venv
.venv\Scripts\activate
pip install aiosmtpd python-telegram-bot
```

Настройте переменные в `.env` (файл не коммитим). Скопируйте пример и впишите токен своего бота:
```powershell
copy .env.example .env
notepad .env  # TELEGRAM_TOKEN=...
```

## Запуск
```powershell
python run.py
```

- SMTP слушает `0.0.0.0:25` и сохраняет письма в SQLite.
- Telegram-бот запускает polling и реагирует на команды `/start`, `/inbox`, `/help`, а также на инлайн-кнопки («📄 Ящик пустой», «↻ Обновить», «✏️ Изменить почту», отдельные письма).

Остановка — `Ctrl+C`. Для продакшена заведите systemd-службу, tmux или supervisor.

## Структура данных
- `users`: Telegram-пользователь (ID, имя, дата регистрации).
- `mailboxes`: привязанный временный ящик (адрес, пароль, создан, активен).
- `emails`: каждое входящее письмо (отправитель, тема, текст/HTML, заголовки, метка прочтения).

Если переключаетесь с старой версии, удалите `tempmail.db` перед запуском, чтобы применить новую схему.

## Работа с Git
Репозиторий уже инициализирован. Чтобы выложить код в удалённый Git:
```powershell
git remote add origin <ssh-или-https-URL>
git branch -M main
git push -u origin main
```

На сервере:
```bash
git clone <ssh-или-https-URL> tempmail
cd tempmail
git pull   # для обновлений
```
